ANALYTICS_ADMIN_PASSWORD=change-me
# Optional for durable production storage. When omitted, the app uses SQLite on Node 22
# or a JSON fallback file on older Node runtimes.
DATABASE_URL=postgres://username:password@host/database
